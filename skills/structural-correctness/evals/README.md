# Structural Correctness Evals

Deterministic fixture evals for the `structural-correctness` skill.

These checks do not call live models. They pin the expected shape of good agent outputs for three pressure scenarios: bulk import planning, double-charge verification, and public API retirement.
